if(!project.hasProperty('BINTRAY_PACKAGE')) project.ext.BINTRAY_PACKAGE = 'ui'
if(!project.hasProperty('ARTIFACT_NAME')) project.ext.ARTIFACT_NAME = 'ui'
if(!project.hasProperty('DESCRIPTION')) project.ext.DESCRIPTION = 'Media UI'
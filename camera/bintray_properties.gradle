if(!project.hasProperty('BINTRAY_PACKAGE')) project.ext.BINTRAY_PACKAGE = 'camera'
if(!project.hasProperty('ARTIFACT_NAME')) project.ext.ARTIFACT_NAME = 'camera'
if(!project.hasProperty('DESCRIPTION')) project.ext.DESCRIPTION = 'A facade for making it easy to use the camera in android'
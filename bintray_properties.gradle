if(!project.hasProperty('ARTIFACT_VERSION')) project.ext.ARTIFACT_VERSION = '0.0.5-SNAPSHOT'
if(!project.hasProperty('ARTIFACT_GROUP')) project.ext.ARTIFACT_GROUP = 'com.staticbloc.media'
if(!project.hasProperty('BINTRAY_REPO')) project.ext.BINTRAY_REPO = 'media'
if(!project.hasProperty('BINTRAY_ORG')) project.ext.BINTRAY_ORG = 'staticbloc'